// Until the trusted checkout (Phase 2) is wired, checkout takes no payment and tracking is sample data.
// Flip this in one place when the server order/payment flow replaces the simulation.
export const DEMO_MODE = true;
